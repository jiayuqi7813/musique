# Musique Core (Java 25 + Gradle Kotlin DSL)

这是一个面向**音频核心能力与播放器 API** 的精简版本，不包含 GUI。

## 技术栈

- Java 25 (Toolchain)
- Kotlin JVM
- Gradle (Kotlin DSL)
- Java Sound（提供 WAV/AIFF/AU 等系统支持格式播放）

## 快速开始

```bash
./gradlew test
./gradlew run --args='path/to/file.wav'
./gradlew run --args='https://example.com/audio.wav'
```

## 核心 API

- `AudioPlayer`：加载、播放、暂停、停止、关闭
  - `load(path: Path)`：本地音频
  - `load(uri: URI)`：网络音频资源（HTTP/HTTPS）
- `PlayerState`：播放器状态
- `JavaSoundAudioPlayer`：基于 Java Sound 的默认实现

## 说明

老项目中大量历史依赖（部分仓库/坐标已失效）会显著增加 Java 25 迁移成本。
此版本优先保证：

1. 可编译
2. 可执行
3. 提供稳定的无 GUI 音频核心组件和播放器 API
4. 同时支持本地和网络资源输入

如果后续需要扩展 MP3/FLAC/AAC 等格式，可在此基础上继续增加解码模块。
